describe("회원 가입 테스트", () => {
  beforeEach(() => {
    // called before every tests / initialize here
  })

  afterEach(() => {
    // called after every tests / deinitialize here
  })

  test("회원 이름은 문자만 허용한다.", () => {
    console.log("될까")
  })
})
